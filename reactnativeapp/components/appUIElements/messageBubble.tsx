import { ThemedView, ThemedViewProps } from "../ThemedView";
import { ThemedText, ThemedTextProps } from "../ThemedText";

export function messageBubble({message, date}: {message : string, date: Date})
{

    //remember to add a style to size the message bubble and time properly
    return (<ThemedView>
        
    </ThemedView>

    )
}